#!/usr/bin/env python

from setuptools import setup

setup(name='DiRAC Metadata Wrapper',
      version='0.1',
      description='Module for adding metadata to existing simulation outputs',
      author='John Helly',
      author_email='j.c.helly@durham.ac.uk',
      packages=['metadata_wrapper',],
      scripts=[],
     )
